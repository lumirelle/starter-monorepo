{
  "name": "playground",
  "type": "module",
  "private": true,
  "scripts": {
    "play": "bun run ./src/index.ts",
    "typecheck": "tsc --build --noEmit"
  },
  "devDependencies": {
    "pkg-placeholder": "workspace:*"
  }
}
